/* using EstructurasLibrary;

INotifier notifier = new EmailNotifierDecorator();

notifier = new SMSDecorator(notifier);

notifier = new WhatsAppDecorator(notifier);

notifier.Send("Servidor caído"); */